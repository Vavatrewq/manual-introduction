export default interface TypeSection {
  before: JSX.Element
  after: JSX.Element
  fap_logo: string
  level: string
  generic_img: string
  video: string
  text_rest: string[]
  title_second: string[]
}
